These folders contain the full evaluation datasets. The evaluation datasets used in the challenge were limited by leaving out certain measurements. 
The full datasets contain all of the measured values.

Each folder includes the reference data, named ref.mat. This is the same dataset for each difficulty level.