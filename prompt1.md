仿真合并
验证集来源

我现在有1000条level_1的仿真数据，我想在level_1上训练fcunet，训练数据为仿真数据的100/200/400/800，验证集、测试集为仿真数据的100，查看测试集效果随数据规模的变化曲线，你觉得可行吗？


@scripts\data_scaling_experiment.py 中的学习率调度机制和早停机制是否会同步调整？

为什么 @scripts\data_scaling_experiment.py 结果保存的文件夹名全部为 @results\fcunet_scaling_n100_{num} ，n100不应该根据--train-sizes动态调整吗？

你可以创建一个脚本对 @results\fcunet_scaling_n{num}_{num} 中之前的训练记录'training_log.json'进行patience=15的早停截断吗？

我发现训练过程中验证时间似乎比训练时间还要久，这是为什么？score计算是不是比较费时？


创建新脚本实现将 @dataset 中已生成的数据改为.h5格式（支持命令行参数指定数据路径，因为要在colab上使用）。
将 @scripts\generate_data.py 中生成数据改成.h5格式，支持向.h5文件中继续添加新数据。
修改 @scripts\data_scaling_experiment.py @scripts\train.py @scripts\evaluate_all.py 支持对.h5格式的数据进行加载处理。

创建新脚本实现将 @dataset 中已生成的数据（支持单样本.npy和多样本.npy）继续写入.h5格式数据文件（可能不存在，也可能已有文件内容）中（支持命令行参数指定数据路径，因为要在colab上使用）。
将 @scripts\generate_data.py 中生成数据机制改成：1、比如 8 个 Worker（多进程）在内存里疯狂算数据。2、算出来的结果不要立刻写盘，而是暂存在每个 Worker 自己内存的 List 里。3、当某个 Worker 的 List 攒够了 1000 个样本（大约也就占用一两百 MB 内存），它就一次性把这 1000 个样本打包成一个稍微大一点的 .npy 或 .npz 写到硬盘里。4、等所有进程跑完，你得到了 100 个包含了 1000 个样本的中型文件。此时调用前面写的新脚本，花几分钟时间，极其顺滑地把这 100 个文件一口气导入到最终的 .h5 文件中。
修改 @scripts\data_scaling_experiment.py @scripts\train.py @scripts\evaluate_all.py 支持对.h5格式的数据进行加载处理。

=写入云盘的通信瓶颈


请尝试根据 @GUIDE\data_generation_optimization.md 中**K. GPU 预处理共轭梯度法（CG）替代 CPU 直接求解**的思路，对 @scripts\generate_data.py 中的forward步骤进行Batched PCG加速。先不要对原脚本进行修改，创建一个新脚本进行尝试，将优化后效果和当前效果进行对比，并不断进行优化尝试，直到效果符合预期为止。


转存机制

gm_reco中的文件内容是什么？为什么比gt和measurements大这么多？

@scripts\npy_to_hdf5.py 是如何将.npy文件写入.h5文件的？它是直接将.npy文件接在.h5已有最大索引的最后面吗？还是会按照文件名写入对应索引？

KTC2023有没有提供32个电极的位置信息？或者其他测量条件信息？
神经网络的预测结果是真实电导率还是3值标签？


=数据质量（数量、形状）
=数据数量
=sim to real(噪声)
神经网络

把固定系统误差的模拟设置为命令行参数选项，以便课程学习或其他模拟需求

将每个电极的76个测量值和角度余弦、正弦合起来作为78个输入值，乘上一个78*(d-2)的K/V得到d-2维向量后再拼接角度余弦、正弦，然后将每个位置的归一化横纵坐标、与中心点距离、角度余弦正弦共5个值输入一个MLP中得到d-5维向量后再拼接归一化横纵坐标、与中心点的归一化距离、角度余弦正弦得到q。(拆分成多头)将q点乘k除以根号d再softmax后乘上V得到最终d张N*N特征图。然后卷积、最大最小池化、卷积、最大最小池化等等，然后反卷积等等，并且上下采样中同尺寸特征图进行连接，即U-Net，最后得到一张预测的图像。是这样吧？

添加代码，实现 @update7.md 中的模型架构，并接入到 @scripts\train.py @scripts\evaluate_all.py 中。

第1个电极在水箱正上方，左右边界为 \pm 2.8125°，然后沿逆时针方向每隔11.25°依次排列后续电极，电极与电导率像素的角度表示最好能对齐。
数据集中从level_1到level_7会逐级移除相应数量的电极对，需要兼容输入数据缺失的情况，当前神经网络是否能鲁棒处理这种情况？

这个保存机制有问题吧，我运行了
python scripts/generate_data.py --num-images 200 --workers 4 --start-idx 0 --measurements-only报错
python scripts/generate_data.py --num-images 400 --workers 4 --start-idx 0 --measurements-only完成运行
为什么只有150个样本？

能不能将Adam优化器改成AdamW？如何调整学习率？

给 @scripts\train.py 添加训练集/验证集/测试集的8:1:1划分，并且使用验证集损失调度学习率和早停。将Adam优化器改成AdamW。

将训练阶段的阶段一缩减至10个epoch，移除阶段二（冻结注意力层），直接进入阶段三（解冻全参数训练）。

由于训练收敛速度太慢，请结合 @update11.md 中的思路帮我改进优化一下。

先添加一个VQ-VAE自编码器训练对电导率图像进行重建，将每张图像编码到低维特征，再解码回原始图像，损失是重构误差。指定其中一个特征维度为角度，编码时先旋转再压缩，解码时先扩展再旋转。
然后使用自编码器对官方示例数据和测试数据集进行重建，将重建图像和真实图像进行对比。
然后使用训练好的自编码器将电导率图像转换为编码向量保存为.h5文件。
然后添加一个神经网络训练其通过电极测量数据预测编码向量，然后将预测编码向量输入解码器就可以得到预测图像。（可能可以考虑将自编码器也解冻加入训练）

@overall.md 中是目前的几个idea，由于前两种方法的逐像素编码过于离散，和电导率图像低频特征不符，故已被舍弃。请你按照 @GUIDE\nn_3.md 中的思路继续尝试实现一下 SAE 方法。

MLP神经网络的误差并不很大，但是效果还是不好，sae_predictor+sae的解码器的预测图像无论是在训练集、验证集、测试集全部都呈现出和仿真图像完全不在一个域内的某种类似结构。修改直接用MLP神经网络从电极测量值预测低维向量的思路，把解码器接上并冻结，先前向传播到重建图像上计算损失再反向传播到MLP神经网络上计算梯度。将编码的低维向量的损失加上重建图像的误差损失作为总损失，其中重建图像的误差损失的权重在训练过程中不断增大。初期以低维向量为主，后期以重建图像误差为主。

添加一个方法，将sae改成vae，并在训练MLP的predictor时只预测均值并不施加噪声来计算解码图像。其他全部和sae方法一致。

将sae和sae_predictor的预测图像损失改成CE + Dice。

去除第一阶段，直接进行第二阶段。直接解冻解码器，保存的权重包含MLP+解码器，损失直接改成图像损失，使用CE + Dice。sae的损失也改成CE + Dice。


我现在打算尝试一种通过离散字典约束自编码器行为从而保证预测图像规则、低频的方法，具体内容在 @GUIDE\nn_4.md 中，请你实现一下，如果有任何疑问或者觉得不合理的地方请提出。

这个不是vq_vae吗，为什么叫vq_sae，另外这个新方法如何推理自编码的重建图像与实际图像对比图？比如你将 @scripts/visualize_sae_reconstruction.py 的功能扩展一下？

告诉我 @Subjects Data 中的数据内容

创建一个新脚本对比官方评分方式和加速评分方式


目前研究成果总结：
@GUIDE\data_generation_optimization.md 加速仿真数据生成
@GUIDE\phantom_shape_generation.md 仿真数据的物体形状数量模拟，即仿真数据质量
dpcaunet: @GUIDE\update7.md，已舍弃，逐像素离散化处理，与低频图像特征不符。
hcdpcaunet: @GUIDE\HC-DPCA-UNet.md，已舍弃，逐像素离散化处理，与低频图像特征不符。
SAE: @GUIDE\nn_3.md，自编码器重建效果还可以，但是电极测量数据MLP_predictor的重建效果很差（即使解冻解码器）。可能可以考虑扩展SAE的编码特征向量维度；或者将sae改成vae，并在训练MLP_predictor时只预测均值并不施加噪声来计算解码图像（实际上已有该研究的论文，不过数据和代码似乎均未开源）；或者通过生成器、判别器方式对某个部分进行训练确保其重建的是规则的图像；或者将编码器、解码器、编码器、解码器不断堆叠，所有编码器和解码器同步参数（或者以某种方式关联），损失以首尾的原始特征重建损失和编码特征重建损失为主，其他辅助损失，通过多层堆叠放大误差，进行训练；或者你仔细分析问题的根因所在并自发进行调整。
ST-1D-VQ-VAE: @GUIDE\nn_4.md，自编码器重建效果还可以，但是电极测量数据MLP_predictor的重建效果较差（冻结解码器），但图像较规则，可能是某部分复杂性不够，具体原因尚未排查，尚未进行改善尝试。
2D-DCT: 尚未尝试，似乎已有相关研究，未进行具体考证。可考虑尝试。
Implicit SDF: 尚未尝试。
@GUIDE\gemini_chat.md ，包含许多重要的研究过程信息，但是篇幅可能较长。
目前需要完成肺部EIT的立项结题，所以还需要肺部相关数据，至少需要肺部仿真数据的相关研究结果，EIDORS库的相关肺部数据仿真工具已经较为完善，可以考虑使用。目标会议为ICIC (International Conference on Intelligent Computing)。

请根据 @overall.md 中的总结，继续完成研究，直到研究成果达到目标水平为止。完成全部研究后请在 @paper 下遵循学术规范撰写学术论文。你可以查阅、下载任何研究所需要的资料、工具，如果需要我的帮助请告诉我。

不用向我询问任何信息和确认，自动进行决策和实验运行，除非需要我的帮助，或者有更大的硬件需求来运行实验需要我通过云服务器运行后将结果交付给你，你可以暂停并告诉我，否则自动继续进行。


将编码器、解码器、编码器、解码器不断堆叠，所有编码器和解码器同步参数（或者以某种方式关联），损失以首尾的原始特征重建损失和编码特征重建损失为主，其他辅助损失，通过多层堆叠放大误差，进行训练；
将编码器、解码器、编码器、解码器不断堆叠，所有编码器和解码器同步参数独立
